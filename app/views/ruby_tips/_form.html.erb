<%= form_with(model: ruby_tip) do |form| %>
  <% if ruby_tip.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ruby_tip.errors.count, "error") %> prohibited this ruby_tip from being saved:</h2>

      <ul>
        <% ruby_tip.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
